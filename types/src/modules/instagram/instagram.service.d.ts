export declare class InstagramService {
    access_token: string;
    private insUrl;
    test(): Promise<unknown>;
    test1(id: string): Promise<unknown>;
    test2(): Promise<void>;
    private get;
}
